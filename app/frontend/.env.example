# API Base URL
VITE_API_URL=http://localhost:3000

# Disable client-side validation (for testing server validation)
# Set to 'true' to bypass Zod validation in forms and test server-side validation
VITE_DISABLE_CLIENT_VALIDATION=false
