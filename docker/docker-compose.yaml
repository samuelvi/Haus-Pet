version: "3.9"

services:
  apicat_api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    depends_on:
      apicat_db:
        condition: service_healthy
      apicat_redis:
        condition: service_healthy
    container_name: apicat_api
    working_dir: /app
    environment:
      - NODE_ENV=development
      - PORT=3000
      - PERSISTENCE_TYPE=postgres
      - DB_HOST=apicat_db
      - DB_USER=user
      - DB_PASSWORD=password
      - DB_NAME=apicat_db
      - DB_PORT=5432
      - REDIS_HOST=apicat_redis
      - REDIS_PORT=6379
      - AUDIT_DB_URI=mongodb://audit_user:audit_pass@apicat_audit_db:27017/audit_log_db?authSource=admin
    ports:
      - "3000:3000"
    volumes:
      - ..:/app
      - /app/node_modules
    command: sh -c "npm install && ./node_modules/.bin/ts-node-dev src/index.ts"
    restart: unless-stopped

  apicat_worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    depends_on:
      apicat_audit_db:
        condition: service_healthy
      apicat_redis:
        condition: service_healthy
    container_name: apicat_worker
    working_dir: /app
    environment:
      - NODE_ENV=development
      - AUDIT_DB_URI=mongodb://audit_user:audit_pass@apicat_audit_db:27017/audit_log_db?authSource=admin
      - REDIS_HOST=apicat_redis
      - REDIS_PORT=6379
    volumes:
      - ..:/app
      - /app/node_modules
    command: sh -c "npm install && ./node_modules/.bin/ts-node src/worker.ts"
    restart: unless-stopped

  apicat_db:
    image: postgres:14-alpine
    container_name: apicat_db
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=apicat_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d apicat_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  apicat_audit_db:
    image: mongo:6.0
    container_name: apicat_audit_db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=audit_user
      - MONGO_INITDB_ROOT_PASSWORD=audit_pass
    ports:
      - "27017:27017"
    volumes:
      - mongo_audit_data:/data/db
    restart: unless-stopped
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh --quiet
      interval: 5s
      timeout: 5s
      retries: 5

  apicat_redis:
    image: redis:7-alpine
    container_name: apicat_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  mongo_audit_data:
